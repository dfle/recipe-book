{{page-title "Recipes"}}

<h1>Recipe Book</h1>

{{#each @model as |recipe|}}
  <RecipeShortOverview
    @id="{{recipe.id}}"
    @title="{{recipe.title}}"
    @description="{{recipe.description}}"
  />
{{/each}}
