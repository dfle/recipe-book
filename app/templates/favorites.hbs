{{page-title "Favorites"}}

<h1>My Favorite Recipes</h1>

{{#each @model as |recipe|}}
  <RecipeShortOverview
    @id="{{recipe.id}}"
    @title="{{recipe.title}}"
    @description="{{recipe.description}}"
  />
{{/each}}
